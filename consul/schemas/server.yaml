%YAML 1.1
---
"$schema": "http://json-schema.org/draft-06/schema#"
title: consul daemon role
description: |
  Consul daemon, server role
type: object
additionalProperties: false

required:
  - enabled

properties:
  enabled:
    description: |
      Enables consul daemon service
    type: boolean
